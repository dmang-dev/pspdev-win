# --- Upstream clones (fetched by bootstrap-windows.ps1 / the build) ---
# The pspdev fork is cloned here at build time; never commit it into this repo.
/pspdev/
/psptoolchain/
/psptoolchain-allegrex/
/psptoolchain-extra/
/pspsdk/
/psp-packages/

# --- Build output (the installed toolchain, hundreds of MB) ---
/install/
/install-vanilla/
/build/
# Release zips emitted by tools/build-library-bundle.sh and the CI bundle step.
/dist/

# --- Local review / prep artifacts (not part of the published repo) ---
/UPSTREAM-PR.md
/COMMUNITY-LISTS.md

# --- Editor / OS / tooling noise ---
.DS_Store
Thumbs.db
*.log
.rstk/
.claude/
